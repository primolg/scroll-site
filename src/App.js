import { Routes, Route } from "react-router-dom";
//components


function App() {
  return(
    <div>
        {/* <Routes>
            <Route index path="/" element={<Element />} />
        </Routes> */}
    </div>
  );
}

export default App;
